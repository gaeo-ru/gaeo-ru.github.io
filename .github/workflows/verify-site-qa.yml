name: Verify GAEO site QA

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/verify-site-qa.yml"
      - "scripts/site_qa.py"
      - "scripts/ensure_site_metadata.py"
      - "scripts/ensure_site_chrome.py"

permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run staging QA
        run: python scripts/site_qa.py --mode staging
